apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring-staging

# Note: KT Cloud dashboards removed from staging environment
# Only keeping basic monitoring stack configuration